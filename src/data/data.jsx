import React from 'react'
import axios from 'axios'


const data = () => {

  

  return (
      title.map((titulo,id)=>{

        return(
          <li key={i}>
            <h4>Nombre: {}</h4>
          </li>
        )

      })
  )
}

export default data